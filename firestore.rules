// Placeholder rules — production rules added in task 15
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all access during development; full rules come in task 15
      allow read, write: if true;
    }
  }
}
